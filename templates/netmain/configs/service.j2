[Unit]
Description=Configure {{ ifname }} network interface
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/netmain-{{ ifname }}-up.sh
ExecStop=/usr/local/bin/netmain-{{ ifname }}-down.sh
RemainAfterExit=true

[Install]
WantedBy=multi-user.target
